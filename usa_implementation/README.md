# usa_implementation (✅ current)

- Purpose: Uniform Semantic Agent (uSA) loader and core types for projecting agents across runtimes.
- Entry points: `loader.py`, `core/` package, `types_v2.py`.
- Tests: none defined; run python checks per project needs.
- Links: ../ARCHITECTURE.md, ../STATUS.md, ../docs/STANDARDS.md
