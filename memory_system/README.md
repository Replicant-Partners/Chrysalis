# memory_system (✅ current)

- Purpose: Python memory system (episodic/semantic, CRDT merge, gossip, stores).
- Setup: from repo root run `pip install -r memory_system/requirements.txt`.
- Tests: from repo root run `pytest tests/test_memory_system.py`.
- Links: ../ARCHITECTURE.md, ../STATUS.md, ../docs/STANDARDS.md
