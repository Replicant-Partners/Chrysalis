# mcp-servers (✅ current)

- Purpose: TypeScript MCP Layer 1 servers.
- Modules: `cryptographic-primitives` (hash/merkle/Ed25519/BLS/random), `distributed-structures` (DAG/logical time/voting/hashgraph).
- Build: from repo root run `npm run build:mcp` or per-directory `npm install && npm run build`.
- Tests: from repo root run `npm run test:mcp` or per-directory `npm test`.
- Links: ../ARCHITECTURE.md, ../STATUS.md, ../docs/STANDARDS.md
